name: Compile CRC - devenv on runner

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  compile-crc-runner-using-devenv:
    uses: ./.github/workflows/reusable-runner-using-devenv.yml
    with:
      playbook: "recipes/crc.yml"
      out_path: "Projects/crc-org/crc/out"
    secrets: inherit