name: Compile CRC - playbook devenv

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  compile-crc-playbook-devenv:
    uses: ./.github/workflows/reusable-playbook-devenv.yml
    with:
      playbook: "recipes/crc.yml"
      out_path: "Projects/crc-org/crc/out"
      out_name: "crc"
    secrets: inherit
