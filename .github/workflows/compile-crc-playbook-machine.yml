name: Compile CRC - playbook machine

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  compile-crc-playbook-machine:
    uses: ./.github/workflows/reusable-playbook-machine.yml
    with:
      playbook: "recipes/crc.yml"
      out_path: "Projects/crc-org/crc/out"
      out_name: "crc"
    secrets: inherit